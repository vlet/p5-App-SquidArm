#!/bin/sh

set -e

case "$1" in
	upgrade|install-upgrade)
		;;
	abort-upgrade)
		exit 0
		;;
esac

if ! grep -q "^sarm:" /etc/passwd
then
	adduser --system --home /var/lib/sarm --group sarm
fi

exit 0
